конкретное действие GetUserUseCase, AddUserUseCase описывает как нужно что делать 

class GetUserUseCase {
  final UserRepository repository;
  GetUserUseCase(this.repository);
    // call  позволяет использовать класс как функцию вызываем в коде просто GetUserUseCase() или GetUserUseCase().call()
  Future<User> call(String id) => repository.getUser(id);
}